export const APP_COPY = {
  stage: 'QA-003 performance-tuned local-first client',
  subtitle:
    'Android-first onboarding with a cycle-aware dashboard, signed remote bootstrap config, seeded local categories with custom CRUD and merge, canonical merchants plus alias management, reusable local classification rules with optional auto-apply, a weighted local history ranker for suggestions, a canonical local budget engine, local budget create/edit plus quiet-mode threshold alerts, optimized local insights with prior-period comparison, filtered Inbox recovery, bottom-sheet quick classify, a full split-items editor, shared manual add primitives, a local history/detail flow with parser context and notes, a dedicated native-capture diagnostics screen with parser inventory plus redacted sharing, app-owned actionable capture notifications with direct reply plus classify / split / skip persistence, safe native-to-JS capture import plus classify / split deep-link routing, a first-class Settings screen for source apps, privacy-mode preview masking, sync/budget defaults, working local CSV exports, a local backup export hook, a persisted local-first sync client with retry-safe queue visibility plus guest-session and pairing credential setup, active-screen-gated heavy derivations, additive local query indexes, and payload-aware sync batching for slower networks.',
  title: 'UPI Spend Tracker',
} as const;
